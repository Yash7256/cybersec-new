# Cybersec Toolkit — Web UI

React (Vite) frontend for the cybersec-toolkit. Uses Clerk for authentication.

## Run

```bash
npm install
npm run dev       # dev server (uses VITE_CLERK_PUBLISHABLE_KEY from .env)
```

## Build

```bash
npm run build     # outputs to dist/, served by the FastAPI backend
npm run lint
```
